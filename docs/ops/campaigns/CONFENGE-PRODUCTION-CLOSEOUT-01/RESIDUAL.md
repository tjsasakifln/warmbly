# Residual — Warmbly

- Deployed SHA is not `origin/main`. `/live` `/ready` `/health/deps` missing until deploy.
- Owner canary HMAC not yet sent this campaign.
- #47 stays OPEN. Canary cannot close it.
- Public nginx still allowlists only inbound + inbound/health.
